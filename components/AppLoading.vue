<template>
  <div class="fixed inset-0 z-[200] flex items-center justify-center bg-white">
    <div class="flex items-center gap-2" aria-label="Loading" role="status">
      <span class="block w-3 h-3 rounded-full bg-primary animate-bounce [animation-delay:-0.32s]" />
      <span class="block w-3 h-3 rounded-full bg-primary animate-bounce [animation-delay:-0.16s]" />
      <span class="block w-3 h-3 rounded-full bg-primary animate-bounce" />
    </div>
  </div>
</template>
